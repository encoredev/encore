! parse
expectError 'Path parameter "p" must be a string, bool, integer, or encore.dev/types/uuid.UUID.'
err 'path parameter ''p'' must be a string, .+'

-- svc/svc.go --
package svc

import (
	"context"
	"encore.dev/types/uuid"
)

type Struct struct{}

//encore:api public path=/str/:p
func Str(ctx context.Context, p *Struct) error { return nil }
