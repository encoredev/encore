! parse
err 'The second parameter must be a string or a pointer to a named struct.'

-- svc/svc.go --
package svc

import (
    "context"
    "encore.dev/beta/auth"
)

//encore:authhandler
func MyAuth(ctx context.Context, p int) (auth.UID, error) { return "", nil }
