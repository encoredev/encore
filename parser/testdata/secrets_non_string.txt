# Verify that secret fields are of type string

! parse
expectError 'Secrets must be of type string.'
err 'field Foo is not of type string'

-- svc/svc.go --
package svc

var secrets struct {
    Foo int
}
