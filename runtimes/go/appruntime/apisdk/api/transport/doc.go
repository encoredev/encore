// Package transport provides an abstraction over the transport layer
// which allows us to add and read metadata from the transport without
// having to know the underlying transport implementation.
package transport
