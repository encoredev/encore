---
seotitle: "Coming from OpenTofu to Encore"
seodesc: How OpenTofu concepts map onto Encore, what to do with infrastructure you have already provisioned, and how to run both alongside each other.
title: Coming from OpenTofu
subtitle: Mapping OpenTofu configuration onto declarations in your application code
lang: platform
---

<!-- STUB: outline only, hidden from the menu until written.
     Follow the structure of Coming from Terraform (platform/migration/from-terraform).
     Every claim on that page is traced to source or docs before it ships. -->

This guide is being written. In the meantime, [Coming from an IaC tool](/docs/platform/migration/from-iac) covers the parts that
are common to every IaC tool, and [Encore vs OpenTofu](/comparison/opentofu) has a feature-level comparison.

## How it works

Side by side: the same resource in OpenTofu and in Encore.

## Where each setting is configured

OpenTofu concept by OpenTofu concept, and where the equivalent lives in Encore.

## Converting OpenTofu resources

Databases, Pub/Sub, object storage, caches, cron jobs and secrets.

## Using Encore with OpenTofu

Whether the two can run alongside each other, and how.

## Trade-offs

What you give up by moving.
